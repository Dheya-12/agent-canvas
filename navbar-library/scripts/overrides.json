{
 "_doc": "Per-reference corrections to the automated probe. Each entry records a metric the probe measures wrongly on that site, why, and the visual evidence that settles it. Overrides suppress a metric; they never assert a pass.",
 "area17": {
  "skip": [
   "startGutter"
  ],
  "reason": "The probe's brand heuristic picks the leftmost interactive element inside the measured <header>, but AREA 17's brand mark is an oversized graphic that overflows the rail and is not among the header's interactive children. The probe therefore reports the first NAV LINK (x=942 at 1440) as the brand. evidence/area17/desktop-initial.png shows the actual mark at x=16, which is what the reconstruction renders.",
  "verifiedBy": "evidence/area17/desktop-initial.png"
 },
 "studiofreight": {
  "skip": [
   "endGutter"
  ],
  "skipViewports": [
   "tablet",
   "mobile"
  ],
  "reason": "At tablet and mobile the reference header exposes exactly one detectable interactive element (the logo) - its menu control is not an <a>/<button>/[role=button], so the probe's rightmost-interactive figure is the logo's right edge (x=36) rather than the real trailing control. End-gutter is therefore not comparable at those two viewports. Desktop and laptop end-gutter remain enforced.",
  "verifiedBy": "evidence/studiofreight/inspection.json (tablet/mobile items(1)); evidence/studiofreight/mobile-initial.png"
 },
 "_doc2": "'skip' suppresses a metric the probe measures WRONGLY (instrument fault). 'deviations' declares a metric where the reconstruction INTENTIONALLY differs from the reference and says why. A deviation never becomes a pass: it downgrades certification to PASS WITH OBSERVATIONS and is listed in the certification matrix.",
 "obys": {
  "deviations": [
   {
    "metric": "linkFontSize",
    "viewports": [
     "laptop",
     "tablet",
     "mobile"
    ],
    "reason": "The reference scales link type strictly with viewport width, reaching 7.8px at 1024, 5.9px at 768 and 3.0px at 390. That is legible only for its own two-word uppercase labels and fails WCAG for arbitrary generated content. This reconstruction keeps the fluid ramp but floors it at 12px."
   },
   {
    "metric": "height",
    "viewports": [
     "tablet",
     "mobile"
    ],
    "reason": "The reference rail is 34px tall at 768 and 17.5px at 390 because its height is also pure vw. A 17.5px bar cannot hold a touch target (44px minimum). This reconstruction floors the rail at 46px below 860px and moves the links into a panel."
   }
  ],
  "verifiedBy": "evidence/obys/inspection.json; evidence/obys/mobile-initial.png"
 },
 "zajno": {
  "skip": [
   "endGutter",
   "startGutter"
  ],
  "skipViewports": [
   "tablet",
   "mobile"
  ],
  "reason": "At tablet and mobile the reference header expands to wrap unrelated page content (148px tall at 390 with position:relative), so its container box is not the nav bar and its gutters are not comparable. Desktop and laptop gutters remain enforced.",
  "deviations": [
   {
    "metric": "linkFontSize",
    "viewports": [
     "laptop",
     "tablet",
     "mobile"
    ],
    "reason": "Reference type is 0.833vw with no floor: 8.5px at 1024, 6.4px at 768 and 3.25px at 390. The fluid ramp is kept but floored at 11px (13px below 700px) so arbitrary generated labels stay legible."
   },
   {
    "metric": "height",
    "viewports": [
     "tablet",
     "mobile"
    ],
    "reason": "Height follows from the type floor: the reference bar is 24px at 768 and its columns render at 3.25px. Below 700px this reconstruction collapses the columns into a single disclosure stack with a tappable control, which the reference does not provide at all."
   },
   {
    "metric": "endGutter",
    "viewports": [
     "laptop"
    ],
    "reason": "Downstream of the declared type floor: at 1024 the reference renders 8.5px labels while this reconstruction renders 11px, so the trailing column is ~25px wider and the trailing gutter is correspondingly narrower. The column PLACEMENT (50.5% / 67% tracks) matches; only the label width differs."
   }
  ],
  "verifiedBy": "evidence/zajno/inspection.json (mobile items at 3.25px); evidence/zajno/mobile-initial.png"
 },
 "houseofspoils": {
  "skip": [
   "startGutter"
  ],
  "reason": "The reference's first navigation trigger ('Shop', clearly visible at the left edge in evidence/houseofspoils/desktop-initial.png) is not an <a>, <button> or [role=button], so the probe never sees it and reports the SECOND item ('Artists', x=92) as the leading element. The leading gutter therefore measures the wrong element on this reference. The trailing gutter, which ends at a real link, remains enforced.",
  "verifiedBy": "evidence/houseofspoils/desktop-initial.png"
 },
 "stinkstudios": {
  "skip": [
   "height",
   "startGutter",
   "endGutter"
  ],
  "reason": "The probe's container heuristic selects the reference's inner <nav> (the right-aligned link group, 927x30 at 1440) rather than the header bar that wraps it, because the header itself scores lower. Height and both gutters therefore describe the link group, not the bar, and are not comparable with a full-width reconstruction. Link type scale, top offset and hide-on-scroll response remain enforced and all match.",
  "verifiedBy": "evidence/stinkstudios/inspection.json (container 927x30 while its first link sits at x=1076); evidence/stinkstudios/desktop-initial.png"
 },
 "mercimichel": {
  "skip": [
   "startGutter"
  ],
  "reason": "At desktop, laptop and tablet the reference's brand mark is not an <a>/<button>/[role=button], so the probe reports the FIRST NAV LINK ('About', x=1123 at 1440) as the leading element. The reference's own mobile viewport, where the mark IS a link, puts it at x=45 - which is exactly the inset this reconstruction uses at every width.",
  "verifiedBy": "evidence/mercimichel/inspection.json (mobile items include the mark at x=45 while desktop items do not)"
 },
 "mvsm": {
  "skip": [
   "height",
   "startGutter",
   "endGutter"
  ],
  "skipViewports": [
   "mobile"
  ],
  "reason": "At 390px the probe selects the reference's inner <nav class=main-nav> (374x20 at y=96) rather than the header that wraps it, because the header scores lower once its links wrap. Height and gutters at that viewport therefore describe the link row, not the bar. Desktop, laptop and tablet all measure the header and remain enforced.",
  "verifiedBy": "evidence/mvsm/inspection.json (mobile container is NAV.main-nav at y=96 while the other viewports are HEADER.header at y=0)"
 },
 "lando": {
  "skip": [
   "startGutter",
   "endGutter"
  ],
  "skipViewports": [
   "tablet",
   "mobile"
  ],
  "reason": "CAPTURE ARTEFACT, not a property of the reference. The tablet pass opened the reference's menu, which does not close on Escape, and the original harness did not reload between viewports - so the tablet and mobile 'initial' records contain the OPEN menu (their item lists include the menu's own links and a Close control). Gutters at those two viewports therefore measure menu content rather than the bar. The harness now reloads between viewports after any pass that opened a menu; this reference's stored evidence predates that fix. Heights, which are unaffected, remain enforced and match exactly at all four viewports.",
  "verifiedBy": "evidence/lando/inspection.json (mobile initial items include 'HomeHome', 'On TrackOn Track' and 'Close menu')"
 },
 "bonhomme": {
  "skip": [
   "startGutter",
   "endGutter"
  ],
  "skipViewports": [
   "mobile"
  ],
  "reason": "At 390px the two sides' leading elements are different controls, so the gutter figures are not comparable. The reference centres an IMAGE mark at x=124, which the probe's brand heuristic selects because it is image-bearing and sits inside the leading third; the reconstruction's mark is a text wordmark at the same centred position, so the heuristic falls back to the leftmost control - the menu button at x=16. Both lay out identically (menu button leading, mark centred, cart trailing). Heights at all four viewports remain enforced and match.",
  "verifiedBy": "evidence/bonhomme/inspection.json (mobile logo IMG at x=124 alongside nm-mobile-menu-button at x=16)"
 },
 "offf": {
  "skip": [
   "height"
  ],
  "skipViewports": [
   "desktop",
   "laptop"
  ],
  "reason": "The probe selects the reference's pinned-layer wrapper, which is 167px tall, but the navigation blocks inside it end at y=96 (two 20px strip rows plus a 56px action block - see the per-element extents in the evidence). The remaining 71px is empty and shows the hero through it, so the container height is not the bar height. The reconstruction matches the BLOCK geometry: 20px strips and a 56px action block. Tablet and mobile heights measure the real collapsed bar and remain enforced.",
  "verifiedBy": "evidence/offf/inspection.json (desktop items end at y=40+56=96 inside a 167px container); evidence/offf/desktop-initial.png"
 },
 "koto": {
  "skip": [
   "startGutter",
   "endGutter",
   "height"
  ],
  "reason": "The per-reference selector for this site (div.flex.h-12) isolates the LINK GROUP, not the bar. The reference composes its navigation from three separate floating groups inset 16px - mark plus toggle at 151x48@16,16, links at 452x48@132,16, and a clock group at 370x48@1428,16 - and the diagnostic could isolate only the middle one; there is no element wrapping all three. So the measured container spans x=132..584 rather than the full bar, and its gutters describe the link group. At tablet and mobile that selector does not match at all and the generic scorer falls back to an unrelated 418px/485px element, so heights there are not comparable either. What IS comparable and does match: the 48px group height and the 16px top inset at desktop and laptop.",
  "verifiedBy": "reports/unresolved.json (koto entry, three-group geometry); evidence/koto/desktop-initial.png"
 }
}